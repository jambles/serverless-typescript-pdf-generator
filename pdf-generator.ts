export const generatePdf = (event, context, callback) => {
    const response = {
        statusCode: 200,
        body: JSON.stringify({
            message: 'not yet impld'
        })
    };

    callback(null, response);
};
